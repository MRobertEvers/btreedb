#ifndef BINARY_SEARCH_H_
#define BINARY_SEARCH_H_

/**
 * @brief Returns location in array where value should be inserted.
 *
 * @param arr
 * @param num_keys
 * @param value
 * @return int
 */
int binary_search(int* arr, unsigned char num_keys, int value, char* found);

#endif